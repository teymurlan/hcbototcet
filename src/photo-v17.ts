import { APP } from './app-v16';
import { ADMIN_APP } from './admin-v17';
import photoV15, { AppState as BaseAppState } from './photo-v15';
import type { Env } from './photo-v15';
export type { Env } from './photo-v15';

const API=(t:string)=>`https://api.telegram.org/bot${t}`;
const MAX_FILE=20*1024*1024,MAX_FILES=20,MIN_FILES=2,GEO_MAX=50;
type Ctx={waitUntil?(p:Promise<any>):void};
type TgUser={id:number;first_name?:string;last_name?:string;username?:string};
type Employee={id:number;name:string;role:string;status:'active'|'blocked';addedAt:number};
type MediaDraft={id:string;type:'photo'|'video';fileId:string;name:string;size:number;addedAt:number};
type EventItem={type:string;label:string;at:number;userId:number;meta?:Record<string,unknown>};

export default{
 async fetch(req:Request,env:Env,ctx?:Ctx):Promise<Response>{
  const u=new URL(req.url);
  if(req.method==='GET'&&(u.pathname==='/'||u.pathname==='/app'))return html(APP);
  if(req.method==='GET'&&u.pathname==='/admin')return html(ADMIN_APP);
  if(req.method==='POST'&&u.pathname==='/webhook')return webhook(req,env,u.origin);
  if(req.method==='POST'&&u.pathname==='/api/media/draft')return uploadDraftFast(req,env,ctx);
  if(req.method==='POST'&&u.pathname==='/api/session/start')return startJobV17(req,env,ctx);
  if(req.method==='POST'&&u.pathname==='/api/before')return submitBefore(req,env,ctx);
  if(req.method==='POST'&&u.pathname==='/api/after')return submitAfter(req,env,ctx);
  if(req.method==='POST'&&u.pathname==='/api/problem')return problemV17(req,env,ctx);
  if(req.method==='GET'&&u.pathname==='/api/admin/jobs')return adminJobs(req,env);
  if(req.method==='GET'&&u.pathname==='/api/admin/job')return adminJob(req,env);
  if(req.method==='GET'&&u.pathname==='/api/admin/employees')return adminEmployees(req,env);
  if(req.method==='GET'&&u.pathname==='/api/admin/media')return adminMedia(req,env);
  if(req.method==='POST'&&u.pathname==='/api/admin/note')return adminNote(req,env);
  if(req.method==='POST'&&u.pathname==='/api/admin/review')return adminReview(req,env);
  return photoV15.fetch(req,env,ctx);
 }
};

export class AppState extends BaseAppState{
 async fetch(req:Request):Promise<Response>{
  const u=new URL(req.url);
  if(u.pathname==='/archive'&&req.method==='GET'){const job=clean(u.searchParams.get('job'),100),stage=clean(u.searchParams.get('stage'),20);return J({ok:true,files:await this.state.storage.get<MediaDraft[]>(`archive:${job}:${stage}`)||[]})}
  if(u.pathname==='/archive'&&req.method==='POST'){const x:any=await req.json(),job=clean(x.jobId,100),stage=clean(x.stage,20),files=Array.isArray(x.files)?x.files:[];if(!job||!['before','after'].includes(stage))return J({ok:false},400);await this.state.storage.put(`archive:${job}:${stage}`,files);return J({ok:true})}
  if(u.pathname==='/events'&&req.method==='GET'){const job=clean(u.searchParams.get('job'),100);return J({ok:true,events:await this.state.storage.get<EventItem[]>(`events:${job}`)||[]})}
  if(u.pathname==='/events'&&req.method==='POST'){const x:any=await req.json(),job=clean(x.jobId,100),item=x.item as EventItem,once=!!x.once;if(!job||!item?.type)return J({ok:false},400);const k=`events:${job}`,a=await this.state.storage.get<EventItem[]>(k)||[];if(!(once&&a.some(v=>v.type===item.type))){a.push(item);await this.state.storage.put(k,a.slice(-120))}return J({ok:true})}
  if(u.pathname==='/job-meta'&&req.method==='GET'){const job=clean(u.searchParams.get('job'),100);return J({ok:true,note:await this.state.storage.get<string>(`note:${job}`)||'',review:!!await this.state.storage.get<boolean>(`review:${job}`)})}
  if(u.pathname==='/job-meta'&&req.method==='POST'){const x:any=await req.json(),job=clean(x.jobId,100);if(!job)return J({ok:false},400);if('note'in x)await this.state.storage.put(`note:${job}`,clean(x.note,1000));if('review'in x)await this.state.storage.put(`review:${job}`,!!x.review);return J({ok:true})}
  if(u.pathname==='/dashboard/jobs'){const jobs=await allJobs(this.state);const enriched=await Promise.all(jobs.map(async j=>({...j,review:!!await this.state.storage.get<boolean>(`review:${j.id}`)})));const stats={total:enriched.length,active:enriched.filter(j=>j.stage!=='done').length,done:enriched.filter(j=>j.stage==='done').length,review:enriched.filter(j=>j.review).length};return J({ok:true,jobs:enriched,stats})}
  if(u.pathname==='/dashboard/job'){const id=clean(u.searchParams.get('id'),100),jobs=await allJobs(this.state),job=jobs.find(j=>String(j.id)===id);if(!job)return J({ok:false,error:'Заявка не найдена'},404);const [before,after,events,note,review]=await Promise.all([this.state.storage.get<MediaDraft[]>(`archive:${id}:before`),this.state.storage.get<MediaDraft[]>(`archive:${id}:after`),this.state.storage.get<EventItem[]>(`events:${id}`),this.state.storage.get<string>(`note:${id}`),this.state.storage.get<boolean>(`review:${id}`)]);return J({ok:true,job,media:{before:before||[],after:after||[]},events:events||[],note:note||'',review:!!review})}
  if(u.pathname==='/dashboard/employees'){const employees=[...(await this.state.storage.list<Employee>({prefix:'employee:'})).values()],jobs=await allJobs(this.state);const out=employees.map(e=>{const own=jobs.filter(j=>Number(j.userId)===Number(e.id)),done=own.filter(j=>j.stage==='done'),active=own.filter(j=>j.stage!=='done'),ds=done.map(j=>Math.max(0,Number(j.finishedAt||0)-Number(j.startedAt||0))).filter(Boolean),avg=ds.length?Math.round(ds.reduce((a,b)=>a+b,0)/ds.length):0;return{...e,activeJob:active[0]||null,stats:{done:done.length,active:active.length,avgDuration:avg?durText(avg):'—'}}});return J({ok:true,employees:out})}
  if(u.pathname==='/ui'&&req.method==='GET'){const id=Number(u.searchParams.get('id'));return J({ok:true,ui:await this.state.storage.get<any>(`ui:${id}`)||null})}
  if(u.pathname==='/ui'&&req.method==='POST'){const x:any=await req.json();await this.state.storage.put(`ui:${Number(x.id)}`,{messageId:Number(x.messageId),screen:clean(x.screen,40)});return J({ok:true})}
  if(u.pathname==='/ui-pending'&&req.method==='GET'){const id=Number(u.searchParams.get('id'));return J({ok:true,pending:await this.state.storage.get<string>(`uiPending:${id}`)||''})}
  if(u.pathname==='/ui-pending'&&req.method==='POST'){const x:any=await req.json(),k=`uiPending:${Number(x.id)}`;if(x.action)await this.state.storage.put(k,clean(x.action,30));else await this.state.storage.delete(k);return J({ok:true})}
  return super.fetch(req);
 }
}

async function allJobs(state:DurableObjectState){const current=[...(await state.storage.list<any>({prefix:'job:'})).values()],history=[...(await state.storage.list<any>({prefix:'history:'})).values()],m=new Map<string,any>();for(const j of history.concat(current)){if(!j?.id)continue;const old=m.get(String(j.id));if(!old||Number(j.finishedAt||j.startedAt||0)>=Number(old.finishedAt||old.startedAt||0))m.set(String(j.id),j)}return[...m.values()].sort((a,b)=>Number(b.finishedAt||b.startedAt||0)-Number(a.finishedAt||a.startedAt||0)).slice(0,500)}

async function startJobV17(req:Request,env:Env,ctx?:Ctx){let body:any;try{body=await req.clone().json()}catch{return J({ok:false,error:'Некорректные данные'},400)}const accuracy=num(body?.accuracy),lat=num(body?.lat),lon=num(body?.lon);if(!coords(lat,lon))return J({ok:false,error:'Не удалось определить местоположение. Разрешите доступ к геопозиции.'},400);if(!accuracy||accuracy>GEO_MAX)return J({ok:false,error:'Не удалось достаточно точно определить адрес. Подойдите к окну или выйдите на открытое место и повторите.'},400);let place:string;try{place=await reversePlaceStrict(lat,lon)}catch{return J({ok:false,error:'Не удалось подтвердить адрес по геопозиции. Проверьте GPS и повторите.'},400)}const r=await photoV15.fetch(req.clone(),env,ctx),x:any=await r.clone().json().catch(()=>null);if(r.ok&&x?.job){x.job.startPlace=place;await putJob(env,x.job);await event(env,x.job.id,'start','Начал фотоотчёт',Number(x.job.userId),true);return J(x,r.status)}return r}

async function uploadDraftFast(req:Request,env:Env,ctx?:Ctx){const auth=await authenticatedState(req,env);if(!auth.ok)return auth.response;const userId=Number(auth.data?.employee?.id),job=auth.data?.job;if(!Number.isSafeInteger(userId)||!job||job.stage==='done')return J({ok:false,error:'Сначала начните уборку.'},409);let form:FormData;try{form=await req.formData()}catch{return J({ok:false,error:'Не удалось прочитать файл.'},400)}const stage=String(form.get('stage')||'');if(!['before','after'].includes(stage))return J({ok:false,error:'Неизвестный этап.'},400);if(stage==='before'&&job.stage!=='started')return J({ok:false,error:'Этап ДО уже закрыт.'},409);if(stage==='after'&&job.stage!=='before_sent')return J({ok:false,error:'Сначала отправьте ДО.'},409);const f=form.get('media');if(!(f instanceof File))return J({ok:false,error:'Файл не найден.'},400);if(!f.type.startsWith('image/')&&!f.type.startsWith('video/'))return J({ok:false,error:'Разрешены только фото и видео.'},400);if(f.size<=0||f.size>MAX_FILE)return J({ok:false,error:'Один файл должен быть не больше 20 МБ.'},400);const existing=await drafts(env,userId,stage);if(existing.length>=MAX_FILES)return J({ok:false,error:`Максимум ${MAX_FILES} файлов на этап.`},400);const type:'photo'|'video'=f.type.startsWith('video/')?'video':'photo';let saved:{fileId:string;messageId?:number};try{saved=await saveTelegramDraft(env,userId,f,type)}catch{return J({ok:false,error:'Не удалось сохранить файл. Проверьте интернет и повторите.'},502)}const item:MediaDraft={id:crypto.randomUUID(),type,fileId:saved.fileId,name:clean(f.name,120)||(type==='video'?'video.mp4':'photo.jpg'),size:f.size,addedAt:Date.now()};await stateCall(env,'/draft','POST',{action:'add',userId,stage,item});if(stage==='after')await event(env,job.id,'after_open','Начал добавлять фото/видео ПОСЛЕ',userId,true);if(saved.messageId){const p=tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:userId,message_id:saved.messageId},5000).catch(()=>null);if(ctx?.waitUntil)ctx.waitUntil(p);else void p}return J({ok:true,file:{id:item.id,type:item.type,name:item.name,size:item.size}})}

async function submitBefore(req:Request,env:Env,ctx?:Ctx){const auth=await authenticatedState(req,env);if(!auth.ok)return auth.response;const userId=Number(auth.data?.employee?.id),job=auth.data?.job;if(!Number.isSafeInteger(userId))return J({ok:false,error:'Не удалось определить сотрудника.'},401);if(!job||job.stage!=='started')return J({ok:false,error:'Этап ДО недоступен.'},409);let body:any;try{body=await req.json()}catch{body={}}const files:MediaDraft[]=await drafts(env,userId,'before');if(files.length<MIN_FILES)return J({ok:false,error:`Добавьте минимум ${MIN_FILES} фото или видео ДО.`},400);const admins=adminIds(env);if(!admins.length)return J({ok:false,error:'Администратор ещё не настроен.'},503);const note=clean(body?.defect_note,600),place=job.startPlace||await reversePlaceStrict(Number(job.start_location?.lat),Number(job.start_location?.lon)).catch(()=>null);if(!place)return J({ok:false,error:'Не удалось подтвердить адрес по геопозиции. Повторите определение местоположения.'},400);job.startPlace=place;job.stage='before_sent';job.beforeSentAt=Date.now();job.beforeCount=files.length;job.defectNote=note;job.reminded=false;await putJob(env,job);await stateCall(env,'/archive','POST',{jobId:job.id,stage:'before',files});await event(env,job.id,'before','Отправил фото/видео ДО',userId,true);await clearDraftStage(env,userId,'before');await stateCall(env,'/schedule','POST',{});const afterUrl=String(job.appUrl||new URL(req.url).origin)+(String(job.appUrl||'').includes('?')?'&':'?')+'after=1';const summary=`🟡 <b>ФОТООТЧЁТ ДО</b>\n━━━━━━━━━━━━\n👤 <b>Клиент:</b> ${esc(job.customer)}\n🏠 <b>Объект:</b> ${esc(job.address)}\n🧽 <b>Тип:</b> ${esc(job.type)}\n🧑‍🔧 <b>Клинер:</b> ${esc(job.employeeName)}${job.employeeUsername?' (@'+esc(job.employeeUsername)+')':''}\n🕐 <b>Начало:</b> ${fmt(job.startedAt)}\n📍 <b>Местоположение клинера:</b> ${esc(place)}\n📎 <b>Материалов ДО:</b> ${files.length}\n${note?`⚠️ <b>Дефекты / замечания:</b> ${esc(note)}`:'✅ <b>Дефекты:</b> отдельно не указаны'}\n\n⏳ <b>Статус:</b> уборка в процессе.`;const delivery=(async()=>{for(const a of admins){await sendAlbum(env.TELEGRAM_BOT_TOKEN,a,files);await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:a,text:summary,parse_mode:'HTML'},8000).catch(()=>null)}await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:userId,text:'✅ <b>Фото/видео ДО принято</b>\n\nВыполните уборку по регламенту. После завершения обязательно отправьте фото/видео <b>ПОСЛЕ</b>.',parse_mode:'HTML',reply_markup:{inline_keyboard:[[{text:'📷 Перейти к фото ПОСЛЕ',web_app:{url:afterUrl},style:'success'}]]}},8000).catch(()=>null)})();if(ctx?.waitUntil)ctx.waitUntil(delivery);else void delivery;return J({ok:true,job})}

async function submitAfter(req:Request,env:Env,ctx?:Ctx){const auth=await authenticatedState(req,env);if(!auth.ok)return auth.response;const userId=Number(auth.data?.employee?.id),job=auth.data?.job;if(!Number.isSafeInteger(userId))return J({ok:false,error:'Не удалось определить сотрудника.'},401);if(job?.stage==='done')return J({ok:true,job,report_id:job.reportId||'',duration_text:durText(Math.max(0,Number(job.finishedAt||Date.now())-Number(job.startedAt||Date.now()))),already_done:true});if(!job||job.stage!=='before_sent')return J({ok:false,error:'Сначала отправьте этап ДО.'},409);let body:any;try{body=await req.json()}catch{return J({ok:false,error:'Некорректные данные.'},400)}const lat=num(body?.lat),lon=num(body?.lon),accuracy=num(body?.accuracy);if(!coords(lat,lon)||!accuracy||accuracy>GEO_MAX)return J({ok:false,error:'Не удалось достаточно точно определить адрес завершения. Подойдите к окну или выйдите на открытое место и повторите.'},400);let finishPlace:string;try{finishPlace=await reversePlaceStrict(lat,lon)}catch{return J({ok:false,error:'Не удалось подтвердить адрес завершения по геопозиции. Повторите.'},400)}const files:MediaDraft[]=await drafts(env,userId,'after');if(files.length<MIN_FILES)return J({ok:false,error:`Добавьте минимум ${MIN_FILES} фото или видео ПОСЛЕ.`},400);const admins=adminIds(env);if(!admins.length)return J({ok:false,error:'Администратор ещё не настроен.'},503);const now=Date.now(),dur=Math.max(0,now-Number(job.startedAt||now)),dist=Math.round(haversine(Number(job.start_location?.lat),Number(job.start_location?.lon),lat,lon)),flags:string[]=[];if(dur<15*60*1000)flags.push('слишком короткое время уборки');if(dist>1000)flags.push('место старта и завершения существенно отличается');const reportId='HC-'+new Date(now).toISOString().slice(2,10).replace(/-/g,'')+'-'+String(userId).slice(-4)+'-'+Math.random().toString(36).slice(2,6).toUpperCase();job.stage='done';job.finishedAt=now;job.end_location={lat,lon,accuracy};job.endPlace=finishPlace;job.afterCount=files.length;job.reportId=reportId;await putJob(env,job);await stateCall(env,'/archive','POST',{jobId:job.id,stage:'after',files});await stateCall(env,'/history','POST',{job});await event(env,job.id,'done','Завершил фотоотчёт',userId,true);await clearDraftStage(env,userId,'after');const summary=`🟢 <b>УБОРКА ЗАВЕРШЕНА</b>\n━━━━━━━━━━━━\n👤 <b>Клиент:</b> ${esc(job.customer)}\n🏠 <b>Объект:</b> ${esc(job.address)}\n🧽 <b>Тип:</b> ${esc(job.type)}\n🧑‍🔧 <b>Клинер:</b> ${esc(job.employeeName)}${job.employeeUsername?' (@'+esc(job.employeeUsername)+')':''}\n🕐 <b>Начало:</b> ${fmt(job.startedAt)}\n🏁 <b>Завершение:</b> ${fmt(now)}\n⏱ <b>Время работы:</b> ${durText(dur)}\n📍 <b>Местоположение клинера:</b> ${esc(finishPlace)}\n📎 <b>Материалы:</b> ДО ${job.beforeCount||0} · ПОСЛЕ ${files.length}\n${job.defectNote?`⚠️ <b>Замечания ДО:</b> ${esc(job.defectNote)}\n`:''}${flags.length?'🚩 <b>Требует внимания:</b> '+esc(flags.join('; ')):'✅ <b>Автопроверка:</b> явных несоответствий нет'}\n\n🆔 <b>Отчёт:</b> <code>${reportId}</code>`;if(flags.length)await stateCall(env,'/job-meta','POST',{jobId:job.id,review:true});const delivery=(async()=>{for(const a of admins){await sendAlbum(env.TELEGRAM_BOT_TOKEN,a,files);await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:a,text:summary,parse_mode:'HTML'},8000).catch(()=>null)}await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:userId,text:`✅ <b>Фотоотчёт завершён</b>\n\n👤 ${esc(job.customer)}\n⏱ Время работы: <b>${durText(dur)}</b>\n🆔 <code>${reportId}</code>`,parse_mode:'HTML'},8000).catch(()=>null)})();if(ctx?.waitUntil)ctx.waitUntil(delivery);else void delivery;return J({ok:true,job,report_id:reportId,duration_text:durText(dur),flags})}

async function problemV17(req:Request,env:Env,ctx?:Ctx){const auth=await authenticatedState(req,env);if(!auth.ok)return auth.response;const job=auth.data?.job,userId=Number(auth.data?.employee?.id);const r=await photoV15.fetch(req.clone(),env,ctx);if(r.ok&&job?.id&&Number.isSafeInteger(userId))await event(env,job.id,'problem','Сообщил о проблеме на объекте',userId,false);return r}

async function adminJobs(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;const r=await stateCall(env,'/dashboard/jobs');return J(r)}
async function adminJob(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;const id=clean(new URL(req.url).searchParams.get('id'),100);const r=await stateCall(env,'/dashboard/job?id='+encodeURIComponent(id));if(!r.ok)return J(r,404);r.media={before:(r.media?.before||[]).map(publicMedia),after:(r.media?.after||[]).map(publicMedia)};return J(r)}
async function adminEmployees(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;return J(await stateCall(env,'/dashboard/employees'))}
async function adminNote(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;let x:any;try{x=await req.json()}catch{return J({ok:false,error:'Некорректные данные'},400)}await stateCall(env,'/job-meta','POST',{jobId:clean(x.job_id,100),note:clean(x.note,1000)});return J({ok:true})}
async function adminReview(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;let x:any;try{x=await req.json()}catch{return J({ok:false,error:'Некорректные данные'},400)}await stateCall(env,'/job-meta','POST',{jobId:clean(x.job_id,100),review:!!x.review});return J({ok:true})}
async function adminMedia(req:Request,env:Env){const a=await adminAuth(req,env);if(!a.ok)return a.response;const u=new URL(req.url),job=clean(u.searchParams.get('job'),100),stage=clean(u.searchParams.get('stage'),20),id=clean(u.searchParams.get('id'),100);if(!job||!['before','after'].includes(stage)||!id)return new Response('Bad request',{status:400});const r=await stateCall(env,`/archive?job=${encodeURIComponent(job)}&stage=${encodeURIComponent(stage)}`),item=(r.files||[]).find((v:MediaDraft)=>v.id===id);if(!item)return new Response('Not found',{status:404});const f=await tgTimeout(env.TELEGRAM_BOT_TOKEN,'getFile',{file_id:item.fileId},7000).catch(()=>null),path=f?.result?.file_path;if(!path)return new Response('Media unavailable',{status:404});const c=new AbortController(),t=setTimeout(()=>c.abort(),12000);try{const rr=await fetch(`https://api.telegram.org/file/bot${env.TELEGRAM_BOT_TOKEN}/${path}`,{signal:c.signal});if(!rr.ok)return new Response('Media unavailable',{status:502});const h=new Headers(rr.headers);h.set('cache-control','private, max-age=60');h.set('x-content-type-options','nosniff');return new Response(rr.body,{status:200,headers:h})}finally{clearTimeout(t)}}
function publicMedia(x:MediaDraft){return{id:x.id,type:x.type,name:x.name,size:x.size,addedAt:x.addedAt}}

async function webhook(req:Request,env:Env,origin:string){if(env.TELEGRAM_WEBHOOK_SECRET&&!safeEq(req.headers.get('X-Telegram-Bot-Api-Secret-Token')||'',env.TELEGRAM_WEBHOOK_SECRET))return new Response('Unauthorized',{status:401});let up:any;try{up=await req.json()}catch{return new Response('OK')}const cb=up?.callback_query,m=up?.message,user:TgUser|undefined=cb?.from||m?.from;if(cb?.id&&user?.id){await tgTimeout(env.TELEGRAM_BOT_TOKEN,'answerCallbackQuery',{callback_query_id:cb.id},4000).catch(()=>null);await setUi(env,user.id,Number(cb.message?.message_id||0),'menu');await callback(cb,env,origin).catch(e=>console.error('callback',e));return new Response('OK')}if(!m?.chat?.id||!user?.id)return new Response('OK');const text=String(m.text||'').trim(),cmd=text.replace(/@\w+$/,'').toLowerCase(),pending=await getUiPending(env,user.id);if(pending&&text&&!text.startsWith('/')){await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:m.chat.id,message_id:m.message_id},4000).catch(()=>null);await applyEmployeeAction(env,m.chat.id,user.id,pending,text,origin);return new Response('OK')}if(['/start','/menu','/webapp'].includes(cmd)||text==='🏠 Главное меню'){await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:m.chat.id,message_id:m.message_id},4000).catch(()=>null);await ensureWebhook(env,origin);await home(env,m.chat.id,user,origin);return new Response('OK')}if(cmd==='/rules'||text==='📋 Правила'){await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:m.chat.id,message_id:m.message_id},4000).catch(()=>null);await rules(env,m.chat.id,user,origin);return new Response('OK')}if((cmd==='/admin'||text==='👥 Сотрудники')&&isAdmin(env,user.id)){await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:m.chat.id,message_id:m.message_id},4000).catch(()=>null);await staff(env,m.chat.id,user,origin);return new Response('OK')}return photoV15.fetch(new Request(req.url,{method:'POST',headers:req.headers,body:JSON.stringify(up)}),env)}

async function callback(cb:any,env:Env,origin:string){const user:TgUser=cb.from,chat=cb.message?.chat?.id||user.id,data=String(cb.data||'');if(data==='v17:home')return home(env,chat,user,origin,Number(cb.message?.message_id||0));if(data==='v17:rules')return rules(env,chat,user,origin,Number(cb.message?.message_id||0));if(data==='v17:staff'&&isAdmin(env,user.id))return staff(env,chat,user,origin,Number(cb.message?.message_id||0));if(data==='v17:list'&&isAdmin(env,user.id))return employeeList(env,chat,user,origin,Number(cb.message?.message_id||0));if(isAdmin(env,user.id)&&['add','block','unblock','remove'].some(v=>data==='v17:'+v)){const action=data.split(':')[1];await setUiPending(env,user.id,action);const msg=action==='add'?'Отправьте одним сообщением:\n<code>TELEGRAM_ID Имя Фамилия</code>':`Отправьте Telegram ID сотрудника, которого нужно ${action==='block'?'заблокировать':action==='unblock'?'разблокировать':'удалить'}.`;return editService(env,chat,user.id,Number(cb.message?.message_id||0),`👥 <b>СОТРУДНИКИ</b>\n\n${msg}`,{parse_mode:'HTML',reply_markup:{inline_keyboard:[[{text:'← Назад',callback_data:'v17:staff',style:'primary'}]]}})}}

async function home(env:Env,chat:string|number,user:TgUser,origin:string,messageId?:number){const access=await accessFor(env,user.id);if(!access.allowed)return editService(env,chat,user.id,messageId||0,`🔒 <b>Доступ закрыт</b>\n\nВаш Telegram ID: <code>${user.id}</code>\nПередайте его администратору House Cleaning.`,{parse_mode:'HTML'});await removeReplyKeyboard(env,chat);const job=(await stateCall(env,'/job?id='+user.id)).job||null,launch=await signLaunch(user,env.TELEGRAM_BOT_TOKEN),appUrl=`${origin}/?launch=${encodeURIComponent(launch)}`,label=job&&job.stage!=='done'?'🧹 Открыть текущую уборку':'🧹 Начать фотоотчёт',status=!job||job.stage==='done'?'Готов к новой уборке':job.stage==='started'?'Ожидаются фото ДО':'Уборка в процессе — ожидаются фото ПОСЛЕ';await tgTimeout(env.TELEGRAM_BOT_TOKEN,'setChatMenuButton',{chat_id:chat,menu_button:{type:'web_app',text:'Фотоотчёты',web_app:{url:appUrl}}},6000).catch(()=>null);const kb:any=[[{text:label,web_app:{url:appUrl},style:'success'}],[{text:'📋 Правила и регламент',callback_data:'v17:rules',style:'primary'}]];if(access.admin)kb.push([{text:'👥 Сотрудники и админ-панель',callback_data:'v17:staff',style:'danger'}]);return editService(env,chat,user.id,messageId||0,`🏠 <b>HOUSE CLEANING · РАБОЧИЙ БОТ</b>\n\n👤 <b>${esc(access.employee?.name||displayName(user))}</b>\n📌 ${esc(status)}\n\n<b>Порядок:</b>\n1️⃣ Клиент и адрес\n2️⃣ Фото/видео ДО + дефекты\n3️⃣ Уборка по регламенту\n4️⃣ Фото/видео ПОСЛЕ\n\nНезавершённая уборка сохраняется автоматически.`,{parse_mode:'HTML',reply_markup:{inline_keyboard:kb}})}
async function rules(env:Env,chat:string|number,user:TgUser,origin:string,messageId?:number){const access=await accessFor(env,user.id),launch=await signLaunch(user,env.TELEGRAM_BOT_TOKEN),appUrl=`${origin}/?launch=${encodeURIComponent(launch)}`;const kb:any=[[{text:'🧹 Открыть уборку',web_app:{url:appUrl},style:'success'}],[{text:'← Главное меню',callback_data:'v17:home',style:'primary'}]];return editService(env,chat,user.id,messageId||0,`📋 <b>HOUSE CLEANING · РЕГЛАМЕНТ</b>\n\n<b>Перед началом</b>\n• Проверьте клиента, адрес и тип уборки.\n• Фото ДО делаются до основной уборки.\n• Все обнаруженные повреждения фиксируются ДО.\n\n<b>Во время уборки</b>\n• Поверхности и видимые загрязнения.\n• Полы, плинтусы, углы и труднодоступные места.\n• Санузел и мокрые зоны.\n• Кухонная / рабочая зона.\n• Финальный осмотр объекта.\n\n<b>Фото ПОСЛЕ</b>\n• Покажите реальный готовый результат.\n• Старайтесь повторять ракурсы ДО.\n• Проверьте объект до завершения отчёта.\n\n<b>Проблемы</b>\nПовреждение, спор с клиентом или дополнительная работа — сразу сообщите администратору.\n\n⚠️ Обязательный фотоотчёт является частью внутреннего регламента компании.`,{parse_mode:'HTML',reply_markup:{inline_keyboard:kb}})}
async function staff(env:Env,chat:string|number,user:TgUser,origin:string,messageId?:number){if(!isAdmin(env,user.id))return;const launch=await signLaunch(user,env.TELEGRAM_BOT_TOKEN),adminUrl=`${origin}/admin?launch=${encodeURIComponent(launch)}`;return editService(env,chat,user.id,messageId||0,'👑 <b>УПРАВЛЕНИЕ HOUSE CLEANING</b>\n\nАдмин-панель показывает все текущие и завершённые заявки, фото ДО/ПОСЛЕ и действия сотрудников.',{parse_mode:'HTML',reply_markup:{inline_keyboard:[[{text:'📊 Открыть админ-панель',web_app:{url:adminUrl},style:'success'}],[{text:'➕ Добавить',callback_data:'v17:add',style:'success'},{text:'📋 Список',callback_data:'v17:list',style:'primary'}],[{text:'🚫 Заблокировать',callback_data:'v17:block',style:'danger'},{text:'✅ Разблокировать',callback_data:'v17:unblock',style:'success'}],[{text:'🗑 Удалить',callback_data:'v17:remove',style:'danger'}],[{text:'← Главное меню',callback_data:'v17:home',style:'primary'}]]}})}
async function employeeList(env:Env,chat:string|number,user:TgUser,origin:string,messageId?:number){const r=await stateCall(env,'/employees'),a:Employee[]=r.employees||[],txt=a.length?a.map((x,i)=>`${i+1}. ${x.status==='active'?'🟢':'🔴'} <b>${esc(x.name)}</b> · <code>${x.id}</code>`).join('\n'):'Список пуст.';return editService(env,chat,user.id,messageId||0,`👥 <b>СОТРУДНИКИ</b>\n\n${txt}`,{parse_mode:'HTML',reply_markup:{inline_keyboard:[[{text:'← Назад',callback_data:'v17:staff',style:'primary'}]]}})}
async function applyEmployeeAction(env:Env,chat:string|number,adminId:number,action:string,text:string,origin:string){await setUiPending(env,adminId,'');let result='';if(action==='add'){const p=text.split(/\s+/),id=Number(p.shift()),name=p.join(' ').trim();if(!Number.isSafeInteger(id)||!name)result='❌ Неверный формат. Нужны Telegram ID, имя и фамилия.';else{const employee:Employee={id,name:clean(name,100),role:'cleaner',status:'active',addedAt:Date.now()};await stateCall(env,'/employee','POST',{action:'upsert',employee});result=`✅ Добавлен: <b>${esc(employee.name)}</b> · <code>${id}</code>`;await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:id,text:'✅ Вам открыт доступ к рабочему боту House Cleaning. Отправьте /start.'},6000).catch(()=>null)}}else{const id=Number(text.trim());if(!Number.isSafeInteger(id))result='❌ Нужен цифровой Telegram ID.';else{const r=await stateCall(env,'/employee','POST',{action,id});result=r.ok?`✅ Выполнено для сотрудника <code>${id}</code>`:'❌ Сотрудник не найден.'}}const ui=await getUi(env,adminId),user:TgUser={id:adminId};await staff(env,chat,user,origin,Number(ui?.messageId||0));if(result){const ui2=await getUi(env,adminId);await editService(env,chat,adminId,Number(ui2?.messageId||0),`👑 <b>УПРАВЛЕНИЕ HOUSE CLEANING</b>\n\n${result}\n\nВыберите следующее действие.`,{parse_mode:'HTML',reply_markup:{inline_keyboard:[[{text:'📊 Админ-панель',web_app:{url:`${origin}/admin?launch=${encodeURIComponent(await signLaunch(user,env.TELEGRAM_BOT_TOKEN))}`},style:'success'}],[{text:'➕ Добавить',callback_data:'v17:add',style:'success'},{text:'📋 Список',callback_data:'v17:list',style:'primary'}],[{text:'🚫 Заблокировать',callback_data:'v17:block',style:'danger'},{text:'✅ Разблокировать',callback_data:'v17:unblock',style:'success'}],[{text:'🗑 Удалить',callback_data:'v17:remove',style:'danger'}],[{text:'← Главное меню',callback_data:'v17:home',style:'primary'}]]}})}}

async function editService(env:Env,chat:string|number,userId:number,messageId:number,text:string,extra:any){let id=messageId||Number((await getUi(env,userId))?.messageId||0);if(id){try{await tgTimeout(env.TELEGRAM_BOT_TOKEN,'editMessageText',{chat_id:chat,message_id:id,text,...extra},6500);await setUi(env,userId,id,'menu');return}catch{}}const old=id;const x=await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:chat,text,...extra},6500);id=Number(x?.result?.message_id||0);if(id)await setUi(env,userId,id,'menu');if(old&&old!==id)await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:chat,message_id:old},4000).catch(()=>null)}
async function removeReplyKeyboard(env:Env,chat:string|number){const x=await tgTimeout(env.TELEGRAM_BOT_TOKEN,'sendMessage',{chat_id:chat,text:'Обновляем меню…',reply_markup:{remove_keyboard:true},disable_notification:true},5000).catch(()=>null),id=x?.result?.message_id;if(id)await tgTimeout(env.TELEGRAM_BOT_TOKEN,'deleteMessage',{chat_id:chat,message_id:id},4000).catch(()=>null)}

async function adminAuth(req:Request,env:Env){const s=await authenticatedState(req,env);if(!s.ok)return s;if(!s.data?.admin)return{ok:false,response:J({ok:false,error:'Только для администратора'},403),data:s.data};return s}
async function authenticatedState(req:Request,env:Env){const u=new URL(req.url);u.pathname='/api/state';u.search='';const r=await photoV15.fetch(new Request(u.toString(),{method:'GET',headers:req.headers}),env);let data:any;try{data=await r.clone().json()}catch{return{ok:false,response:r,data:null}}return{ok:r.ok&&data?.ok!==false,response:r,data}}
async function accessFor(env:Env,id:number){const admin=isAdmin(env,id),r=await stateCall(env,'/employee?id='+id),employee:Employee|null=r.employee||null;return{allowed:admin||!!(employee&&employee.status==='active'),admin,employee}}
async function drafts(env:Env,id:number,stage:string){const r=await stateCall(env,`/drafts?id=${id}&stage=${encodeURIComponent(stage)}`);return r.files||[]}
async function clearDraftStage(env:Env,id:number,stage:string){await stateCall(env,'/draft','POST',{action:'clear',userId:id,stage})}
async function putJob(env:Env,job:any){await stateCall(env,'/job','POST',{job})}
async function event(env:Env,jobId:string,type:string,label:string,userId:number,once:boolean){await stateCall(env,'/events','POST',{jobId,item:{type,label,at:Date.now(),userId},once})}
async function stateCall(env:Env,path:string,method='GET',body?:unknown){const id=env.STATE.idFromName('global'),stub=env.STATE.get(id),init:any={method,headers:{'content-type':'application/json'}};if(body!==undefined)init.body=JSON.stringify(body);const r=await stub.fetch('https://state.local'+path,init);return await r.json() as any}
async function getUi(env:Env,id:number){return(await stateCall(env,'/ui?id='+id)).ui||null}async function setUi(env:Env,id:number,messageId:number,screen:string){if(messageId)await stateCall(env,'/ui','POST',{id,messageId,screen})}async function getUiPending(env:Env,id:number){return(await stateCall(env,'/ui-pending?id='+id)).pending||''}async function setUiPending(env:Env,id:number,action:string){await stateCall(env,'/ui-pending','POST',{id,action})}

async function saveTelegramDraft(env:Env,chatId:number,file:File,type:'photo'|'video'){const fd=new FormData();fd.append('chat_id',String(chatId));fd.append(type,file,file.name||(type==='video'?'video.mp4':'photo.jpg'));fd.append('disable_notification','true');const c=new AbortController(),tm=setTimeout(()=>c.abort(),20000);try{const r=await fetch(`${API(env.TELEGRAM_BOT_TOKEN)}/${type==='video'?'sendVideo':'sendPhoto'}`,{method:'POST',body:fd,signal:c.signal}),x:any=await r.json();if(!r.ok||!x.ok)throw new Error('telegram');const msg=x.result,fileId=type==='video'?msg?.video?.file_id:msg?.photo?.[msg.photo.length-1]?.file_id;if(!fileId)throw new Error('file');return{fileId,messageId:Number(msg?.message_id||0)}}finally{clearTimeout(tm)}}
async function sendAlbum(token:string,chat:string,items:MediaDraft[]){for(let i=0;i<items.length;i+=10){const part=items.slice(i,i+10);if(part.length===1){const x=part[0];await tgTimeout(token,x.type==='video'?'sendVideo':'sendPhoto',x.type==='video'?{chat_id:chat,video:x.fileId,supports_streaming:true}:{chat_id:chat,photo:x.fileId},12000).catch(()=>null);continue}const media=part.map(x=>x.type==='video'?{type:'video',media:x.fileId,supports_streaming:true}:{type:'photo',media:x.fileId});await tgTimeout(token,'sendMediaGroup',{chat_id:chat,media},15000).catch(async()=>{for(const x of part)await tgTimeout(token,x.type==='video'?'sendVideo':'sendPhoto',x.type==='video'?{chat_id:chat,video:x.fileId,supports_streaming:true}:{chat_id:chat,photo:x.fileId},9000).catch(()=>null)})}}
async function reversePlaceStrict(lat:number,lon:number){if(!coords(lat,lon))throw new Error('coords');const c=new AbortController(),tm=setTimeout(()=>c.abort(),5500);try{const r=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}&zoom=18&addressdetails=1&accept-language=ru`,{headers:{'user-agent':'HouseCleaningSPB-Control/2.0','accept':'application/json'},signal:c.signal});if(!r.ok)throw new Error('geo');const x:any=await r.json(),a=x?.address||{},road=a.road||a.pedestrian||a.residential||a.neighbourhood||'',house=a.house_number||'',district=a.suburb||a.city_district||'',city=a.city||a.town||a.village||'';if(!road&&!house)throw new Error('no address');return[road&&house?`${road}, ${house}`:road||house,district,city].filter(Boolean).join(', ')}finally{clearTimeout(tm)}}
async function ensureWebhook(env:Env,origin:string){const body:any={url:origin.replace(/\/$/,'')+'/webhook',drop_pending_updates:false,allowed_updates:['message','callback_query']};if(env.TELEGRAM_WEBHOOK_SECRET)body.secret_token=env.TELEGRAM_WEBHOOK_SECRET;await tgTimeout(env.TELEGRAM_BOT_TOKEN,'setWebhook',body,7000).catch(()=>null)}
async function tgTimeout(token:string,method:string,body:any,ms:number){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(`${API(token)}/${method}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body),signal:c.signal}),x:any=await r.json();if(!r.ok||!x.ok)throw new Error(x?.description||'Telegram API error');return x}finally{clearTimeout(t)}}
function adminIds(env:Env){return String(env.ADMIN_IDS||'').split(',').map(x=>x.trim()).filter(Boolean)}function isAdmin(env:Env,id:number){return adminIds(env).includes(String(id))}function displayName(u:TgUser){return[u.first_name,u.last_name].filter(Boolean).join(' ')||u.username||('ID '+u.id)}function clean(v:any,max:number){return String(v??'').replace(/[\u0000-\u001f\u007f]/g,' ').trim().slice(0,max)}function num(v:any){const n=Number(v);return Number.isFinite(n)?n:0}function coords(lat:any,lon:any){const a=Number(lat),b=Number(lon);return Number.isFinite(a)&&Number.isFinite(b)&&a>=-90&&a<=90&&b>=-180&&b<=180&&(a!==0||b!==0)}function esc(x:any){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c))}function fmt(ms:number){return new Intl.DateTimeFormat('ru-RU',{timeZone:'Europe/Moscow',day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(ms))}function durText(ms:number){const m=Math.floor(ms/60000),h=Math.floor(m/60);return h?`${h} ч ${m%60} мин`:`${m} мин`}function haversine(a:number,b:number,c:number,d:number){if(![a,b,c,d].every(Number.isFinite))return 0;const R=6371000,r=(x:number)=>x*Math.PI/180,dp=r(c-a),dl=r(d-b),z=Math.sin(dp/2)**2+Math.cos(r(a))*Math.cos(r(c))*Math.sin(dl/2)**2;return 2*R*Math.asin(Math.sqrt(z))}
async function signLaunch(user:TgUser,token:string){const payload={id:user.id,first_name:user.first_name||'',last_name:user.last_name||'',username:user.username||'',exp:Math.floor(Date.now()/1000)+12*60*60},body=b64(new TextEncoder().encode(JSON.stringify(payload))),sig=hex(await hmac(new TextEncoder().encode(token),new TextEncoder().encode('launch:'+body)));return body+'.'+sig}async function hmac(key:BufferSource,data:BufferSource){const k=await crypto.subtle.importKey('raw',key,{name:'HMAC',hash:'SHA-256'},false,['sign']);return new Uint8Array(await crypto.subtle.sign('HMAC',k,data))}function hex(a:Uint8Array){return[...a].map(x=>x.toString(16).padStart(2,'0')).join('')}function b64(a:Uint8Array){let s='';a.forEach(x=>s+=String.fromCharCode(x));return btoa(s).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}function safeEq(a:string,b:string){if(a.length!==b.length)return false;let x=0;for(let i=0;i<a.length;i++)x|=a.charCodeAt(i)^b.charCodeAt(i);return x===0}
function html(s:string){return new Response(s,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store, no-cache, must-revalidate','x-content-type-options':'nosniff','referrer-policy':'no-referrer','permissions-policy':'camera=(self), geolocation=(self)'}})}function J(data:any,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}})}